import { Swiper as VSwiper, SwiperSlide as VSwiperSlide } from 'swiper/vue'
import { usePlugins } from '@/@core/app'
import 'swiper/swiper-bundle.css'

export default usePlugins({ VSwiper, VSwiperSlide })
